/* ============================================================
   Cloud sync configuration  (optional — leave blank to stay offline-only)

   "Publishable key" is the new Supabase name for the old "anon" key — the PUBLIC,
   browser-safe key. Do NOT use the "Secret key", the "Direct connection string",
   or anything under CLI setup.

   Setup: see README → "Cloud sync setup" (run the SQL once, then sign in per device).
   The publishable key is SAFE to ship publicly: Row Level Security limits each
   signed-in user to only their own row.
   ============================================================ */
window.MT_SUPABASE_URL = 'https://ublgugudwsnfuzlbxsyp.supabase.co';
window.MT_SUPABASE_ANON_KEY = 'sb_publishable_1I8R20lIqMoy7FQl0o5BOw_9ZUmfkHe';
